import React, { FC } from 'react';
import { StyleSheet, Text, View } from 'react-native';

const QuizContentContainer: FC = () => {
  return (
    <View>
      <Text>QuizContentContainer</Text>
    </View>
  );
};

export default QuizContentContainer;

const styles = StyleSheet.create({});
